# Orchestration skill

The orchestration skill coordinates specialist Career OS departments. It does not replace their domain rules. The runtime should keep state, routing, approvals, retries, and auditability deterministic and inspectable.